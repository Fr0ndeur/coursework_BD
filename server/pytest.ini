[pytest]
minversion = 7.0
addopts = --strict-markers --cov=app --cov-report=term-missing --cov-report=html
testpaths = tests
markers =
  integration: narrow integration tests
  contract: API contract tests
